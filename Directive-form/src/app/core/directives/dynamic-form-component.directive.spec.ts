import { DynamicFormComponentDirective } from './dynamic-form-component.directive';

describe('DynamicFormComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicFormComponentDirective();
    expect(directive).toBeTruthy();
  });
});
